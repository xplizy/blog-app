version: '3'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - db
    environment:
      - MONGODB_URI=mongodb://db:27017/blog-db

  db:
    image: mongo:latest
    volumes:
      - db-data:/data/db

volumes:
  db-data: